apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      component: backend
  template:
    metadata:
      labels:
        component: backend
    spec:
      containers:
        - name: backend
          image: omkar5655/backend:latest
          ports:
            - containerPort: 8080
          env:
            - name: PGUSER
              value: postgres
            - name: PGHOST
              value: localhost
            - name: PGPORT
              value: "5432"
            - name: PGDATABASE
              value: loizenaidb
            - name: PASSWORD
              valueFrom:
                secretKeyRef:
                  name: pgpassword
                  key: PGPASSWORD