apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-deployment
spec:
  selector:
    matchLabels:
      component: backend
  template:
    metadata:
      labels:
        component: backend
    spec:
      containers:
        - name: backend
          image: omkar5655/backend:latest
          ports:
            - containerPort: 8080
#          env:
#            - name: database
#              value: loizenaidb
#            - name: username
#              value: postgres
#            - name: password
#              value: "123"
#            - name: host
#              value: postgres-cluster-service
#            - name: dialect
#              value: postgres